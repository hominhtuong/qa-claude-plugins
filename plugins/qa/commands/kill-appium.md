---
description: Kill all running Appium servers using the kill-appium.sh script
allowed-tools: Bash
---

# /kill-appium — Stop Appium

Run the script `scripts/kill-appium.sh` to kill all running Appium server processes. Execute the script using bash and show the output to the user.
